# دليل التشغيل

كل الخطوات من الصفر. خُذ وقتك، الترتيب مهم.

---

## قبل ما تبلّش

الملفات اللي معك:

| الملف | شو بيعمل |
|---|---|
| `index.html` | التطبيق نفسه |
| `manifest.webmanifest` | بيخلّيه ينزّل على الموبايل |
| `sw.js` | بيخلّيه يشتغل بدون إنترنت |
| `icon-192.png` `icon-512.png` `apple-touch-icon.png` `favicon.png` | أيقونات شعارك |
| `supabase-setup.sql` | إعداد قاعدة البيانات |
| ملف الاستيراد الخاص | بيانات العملاء والموظفين والحركات الافتتاحية؛ يُحفظ خارج GitHub لأن المستودع عام |

**لا تغيّر أسماء الملفات ولا تشيل ولا واحد منهم.**

---

## الخطوة ١ — قاعدة البيانات (١٠ دقايق)

١. افتح `supabase.com` واعمل حساب مجاني.

٢. اضغط **New project**. اختار اسم، وحط كلمة سر لقاعدة البيانات — **احفظها بمكان آمن**، ما رح تشوفها مرة تانية. اختار أقرب منطقة إلك (Frankfurt أو Singapore أقرب شي للإمارات). استنى دقيقتين لحد ما يجهز.

٣. من القائمة اليسار اضغط **SQL Editor** ← **New query**.

٤. افتح ملف `supabase-setup.sql` بأي محرر نصوص، انسخ **كل** محتواه، والصقه بالمربّع.

٥. اضغط **Run**. لازم تشوف بالآخر:
> رِواء ستوديو is ready. The first account you create becomes the owner.

إذا طلعت رسائل صفراء مكتوب عليها `NOTICE ... skipping` — هاي طبيعية تماماً، تجاهلها.

٦. من القائمة اليسار **Settings** ← **API**. رح تحتاج شغلتين من هالصفحة بعد شوي:
   - **Project URL** — شكله `https://xxxxxxxx.supabase.co`
   - **anon public** — مفتاح طويل

خلّي الصفحة مفتوحة.

---

## الخطوة ٢ — النشر على الإنترنت (٣ دقايق)

١. حط كل الملفات بمجلّد واحد على سطح المكتب. سمّيه أي اسم.

٢. افتح `app.netlify.com/drop`.

٣. **اسحب المجلّد كله** على الصفحة. مش الملفات وحدة وحدة — المجلّد.

٤. بعد عشر ثواني بيعطيك رابط شكله `random-name-123.netlify.app`.

٥. اعمل حساب مجاني عندهم لتثبيت الرابط، وإلا بينحذف. ومن **Site settings** بتقدر تغيّر الاسم لشي أحلى.

**هاد الرابط هو تطبيقك.** ابعته لشريكك وللمحاسب.

---

## الخطوة ٣ — الربط وأول حساب (دقيقتين)

١. افتح الرابط.

٢. اختار **صاحب الشركة** ← **الإعدادات** ← **السحابة** ← **اربط قاعدة بيانات**.

٣. الصق الـ **Project URL** والمفتاح **anon** اللي جبتهم من الخطوة ١.

٤. رح تفتح شاشة إنشاء حساب. حط إيميلك وكلمة سر.

> **أول حساب بينعمل بيصير صاحب الشركة تلقائياً وبياخد كل الصلاحيات.** تأكد إنه حسابك إنت.

٥. إذا طلبلك تأكيد الإيميل، افتح إيميلك واضغط الرابط، وبعدين ارجع سجّل دخول.

---

## الخطوة ٤ — ارفع بياناتك (مهمة!)

لهذا الإصدار، افتح **الإعدادات ← البيانات ← استيراد** واختر `initial-data-2026-09.json`. راجع أن الصندوق صفر وأن أسماء العملاء ظاهرة، ثم افتح **الإعدادات ← السحابة ← ارفع بيانات هالجهاز عالسحابة**.

اعملها **مرة واحدة فقط** لأنها تستبدل البيانات الموجودة على السيرفر. احتفظ بنسخة تصدير قبلها إذا كان عندك أي أرقام قديمة تريد الرجوع لها.

---

## الخطوة ٥ — شريكك والمحاسب

١. ابعتلهم الرابط.

٢. كل واحد بيفتحه ويعمل حساب بإيميله.

٣. أول ما يعملوا حساب بيشوفوا **"بانتظار الموافقة"** — ما بيقدروا يشوفوا ولا رقم.

٤. إنت بتفتح تبويب **المستخدمين**، بتلاقي أسماءهم، بتضغط **تعديل**:

- اختار قالب جاهز: **الشريك** أو **المحاسب** أو **عرض فقط**
- أو حدّد يدوي بالظبط شو بيفتح وشو بيعدّل
- فعّل **الحساب مفعّل** بالآخر

٥. بيقدروا يدخلوا لحظتها.

### خانة واحدة مهمة

**"بيشوف دخلك الخاص والإيجار"** — اتركها **مطفية**.

هاي مش مجرّد إخفاء من الشاشة. قاعدة البيانات نفسها بترفض ترسل هالبيانات لأي حدا ما عندو هالخانة. جرّبتها: حتى لو حدا فهمان بالبرمجة وطلب البيانات مباشرة من السيرفر، بيرجعله **صفر** نتائج.

---

## الخطوة ٦ — تنزيله على الموبايل

**آيفون** — لازم **سفاري** بالذات، ما بيشتغل بكروم:
افتح الرابط ← زر المشاركة تحت ← انزل ← **إضافة إلى الشاشة الرئيسية**

**أندرويد** — كروم:
افتح الرابط ← النقاط الثلاث فوق ← **تثبيت التطبيق**

بيصير عندك أيقونة بشعارك، وبيفتح ملء الشاشة بدون شريط متصفح — متل أي تطبيق نزّلته من المتجر بالظبط.

---

## أمور عملية

**الكل بيشوف نفس الأرقام لحظياً.** لو المحاسب سجّل دفعة وإنت فاتح التطبيق، الرقم بيتغيّر عندك بدون ما تعمل تحديث.

**بيشتغل بدون إنترنت** — بيفتح ويوريك آخر أرقام. أول ما يرجع النت بيتزامن.

التصميم الحالي خفيف ومناسب لخطة Supabase المجانية: السجل عبارة عن صفوف JSON صغيرة، ولا يعتمد على رفع فيديوهات أو صور إلى Storage، ولا على وظائف دورية مدفوعة. راجع حدود الخطة من لوحة Supabase لأنها قد تتغير مع الوقت.

**المفتاح anon مش سر.** هو مصمّم ليكون علني وبينحط بأي تطبيق ويب. الحماية الحقيقية بقواعد قاعدة البيانات اللي ركّبناها، مش بإخفاء المفتاح.

---

## إذا صار شي غلط

**"ما قدرت أوصل للمشروع"** — تأكد إنك ناسخ الـ URL كامل مع `https://` وبدون `/` بالآخر، وإنك أخدت مفتاح **anon** مش `service_role`.

**عملت حساب وضل يقول بانتظار الموافقة** — لازم صاحب الشركة يفعّلك من تبويب المستخدمين.

**أول حساب طلع مش تبعي** — روح على Supabase ← **Table Editor** ← جدول `profiles`، وغيّر خانة `role` لـ `owner` عند حسابك، وخلي `active` مفعّلة.

**بدي أرجّع كل شي من الصفر** — Supabase ← SQL Editor ← شغّل `supabase-setup.sql` مرة تانية. آمنة تماماً، ما بتمسح بيانات.

---

## تحديث الفواتير والمواعيد والـConnector

بعد تنزيل هذا الإصدار، شغّل ملف `supabase-setup.sql` كاملًا مرة ثانية من **SQL Editor**. الملف آمن للتحديث ولا يحذف البيانات الحالية، وسيضيف جداول الفواتير والرواتب الناقصة والإشعارات وسجل التدقيق.

### ربط ChatGPT

1. من Supabase افتح **Authentication → OAuth Server** وفعّل OAuth 2.1 وDynamic Client Registration.
2. اجعل **Authorization Path** هو `/oauth/consent`، وتأكد أن Site URL هو رابط Ledger المنشور.
3. انشر مجلد `mcp-server` على استضافة Node تدعم HTTPS واحتفظ بمفتاح `service_role` ضمن أسرار الاستضافة فقط.
4. اتبع ملف `mcp-server/README.md` لإضافة رابط `/mcp` داخل ChatGPT.

الدفعات والرواتب في الـConnector هي **تسجيل محاسبي فقط**؛ لا ينفّذ تحويلًا بنكيًا. الحذف يحتاج تأكيدًا صريحًا، وكل تعديل يُحفظ في `audit_log`.

### إشعارات المواعيد

الموعد يحفظ اسم العميل ووسيلة الاتصال. عند طلب إرسال إشعار، يوضع في جدول `notifications`. لإرساله فعليًا عبر واتساب أو البريد أو SMS، اربط `NOTIFICATION_WEBHOOK_URL` مع مزود الإرسال أو مع Make / Zapier / n8n.
